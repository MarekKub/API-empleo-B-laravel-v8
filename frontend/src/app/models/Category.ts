/**
 *    Modelo / Entidad Category que mapea la tabla de 'categories'
 * @author Marek Kubicki
 * @version 1.0.0
 */
export class Category{
    constructor(
        public id: number,
        public name: string
    ){}
}